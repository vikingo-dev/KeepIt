---
import "@styles/globals.css";
import { configSite } from "config";
import { App } from "@components/App";
import Layout from "@layouts/Layout.astro";
---

<Layout title={configSite.title + " - " + configSite?.descrSmall}>
  <main>
    <App client:load />
  </main>
</Layout>
